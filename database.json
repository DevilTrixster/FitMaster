{
  "defaultEnv": "development",
  "development": {
    "user": "${DB_USER}",
    "password": "${DB_PASSWORD}",
    "host": "${DB_HOST}",
    "port": "${DB_PORT}",
    "database": "${DB_NAME}",
    "dir": "src/infrastructure/database/migrations",
    "table": "pgmigrations",
    "checkOrder": true
  }
}